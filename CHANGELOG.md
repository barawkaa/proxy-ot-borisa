# Changelog

## v4.0.1 — trusted-source unblock fix

- Исправлен критичный баг v4.0.0: внешний sing-box inbound принимал подключения от trusted-роутера, но раннее правило источников отправляло весь трафик в `outbound/block`, из-за чего не работали даже HTTP/ChatGPT/Google. В логах это выглядело как `inbound/http[IN-HTTP-2081] from 188.143.204.77` → `outbound/block[block]` → `operation not permitted`.
- Trusted IP из обычного реестра `trusted_clients.json` теперь автоматически добавляются в trusted transport CIDR и в allowed source CIDR. Это защищает Keenetic/роутер от блокировки source-country/unknown-source правилом.
- Trusted-only direct mode теперь включается не только при заполненном `security.trusted_auth_bypass_cidrs`, но и при наличии trusted-клиентов в `trusted_clients.json`.
- Добавлен regression-test: trusted public router `188.143.204.77` не должен быть исключён из allowed source CIDR и не должен попадать под общий ранний block-rule.

## v4.0.0 — sing-box-first transport rebuild

- Переписана транспортная архитектура HTTP/SOCKS: публичные порты `2080` и `2081` теперь слушает сам `sing-box` напрямую.
- Python backend больше не запускает внешний HTTP/SOCKS TCP relay и не прокачивает SOCKS5-потоки. Это убирает класс ошибок старой архитектуры: `unsupported command 5`, `idle timeout`, `peer reset`, `socket already closed` на участке Python gateway → internal sing-box.
- Сохранены внутренние localhost-inbounds `12080/12081` только для диагностики и внутренних проверок; пользовательский трафик Keenetic/браузера идёт сразу в `sing-box`.
- Trusted bypass реализован в режиме trusted-only: когда включён bypass и заданы trusted CIDR, внешний sing-box inbound работает без proxy-auth, но источники вне private/trusted CIDR блокируются ранним route-rule. Это честная замена невозможной комбинации «auth и no-auth на одном sing-box inbound».
- Для внешних HTTP/SOCKS inbounds включён native sniffing sing-box, чтобы IP-first SOCKS5-сценарии от Keenetic могли маршрутизироваться по SNI/домену без Python-перехвата TLS.
- Сохранена нормализация Hiddify/Clash/Xray uTLS fingerprints (`helloChrome_120` → `chrome`, `HelloFirefox_Auto` → `firefox`) до генерации runtime `sing-box.json`.
- Dockerfile закрепляет sing-box на `ghcr.io/sagernet/sing-box:v1.12.12`, без `sing-box:latest`.
- Добавлены/обновлены продуктовые проверки новой архитектуры: внешние порты принадлежат sing-box, внутренние порты остаются localhost, Python gateway не стартует, OpenAI-домены расширяются, unsupported fingerprints не попадают в generated config.

## 1.26.1

- Переработана диагностика URL: теперь результат разбит на этапы TCP/CONNECT, TLS, HTTP-запрос, HTTP-ответ, чтение данных и закрытие соединения.
- Исправлена причина ложной ошибки `SSLError: RECORD_LAYER_FAILURE` на сайтах вроде Instagram, когда HTTP 200 и тело ответа уже получены: диагностическое чтение теперь корректно определяет завершение chunked-ответа и не валит проверку на позднем TLS-close после данных.
- UI диагностики URL теперь показывает этапы по каждому участку: HTTP 2081, SOCKS5 2080 и Internal VPN-core.
- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP, SNI/Re:filter routing и relay длинных потоков не переписывались.

## 1.26.0

- Продуктовая стабилизация после серии 1.25.x: клиенты, соединения, диагностика, логи и документация приведены к более аккуратному публичному состоянию.
- Соединения группируются по входу/назначению/маршруту: одинаковые домены больше не выглядят как бессмысленные дубли, а отдельные IP/CDN-адреса показываются в колонке **IP / исходный адрес**.
- Нераспознанные подключения и сканеры отделены от настоящих зарегистрированных/trusted-клиентов; fallback-попытки с IP уже известного клиента скрываются из технического блока, не удаляя историю и не меняя доступ.
- В UI оставлены короткие метки `SNI`, `SNI + Re:filter`, `LAN`, `неподтв.` вместо длинных отладочных пояснений под каждой строкой.
- `proxy-gateway`, служебные raw-hop соединения и короткие unknown-события не попадают в «Основные сайты».
- Gateway/relay логи классифицируют штатные закрытия соединений спокойнее и не должны засорять errors обычными `Bad file descriptor` / `Connection reset by peer` / idle timeout.
- Smoke-test расширен проверкой согласованности версии во всех публичных файлах и наличия ключевых UI-механизмов 1.26.0.
- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP/CIDR, SNI и Re:filter routing не переписывались.

## 1.25.21

- Добавлена колонка **IP / исходный адрес** в таблицы соединений и диагностики реального клиента.
- Сокращены многострочные SNI-пояснения в списках соединений: исходный IP вынесен в отдельную колонку.
- Отображение маршрута gateway-соединений теперь учитывает фактический outbound sing-box по скрытым raw-hop строкам, чтобы HTTP/SOCKS домены из Re:filter не показывались ложным DIRECT.
- Локальные IP не скрываются: они остаются видимыми как LAN / локальный адрес.
- HTTP 2081, SOCKS5 2080, trusted IP/CIDR, relay и SNI-sniff транспортно не переписывались.

## 1.25.20

- Улучшено отображение соединений клиента: теперь в основной таблице явно показывается вход `HTTP proxy`, `SOCKS5 proxy` или другой gateway-вход, а технический протокол `tcp` вынесен в малую строку.
- Локальные IP назначения, например `192.168.1.1`, теперь отображаются как `LAN / локальный адрес`, чтобы не путать их с внешним `DIRECT` или неподтверждённым VPN-маршрутом.
- Диагностика задержек больше не показывает старую внутреннюю ошибку `name 'server_version' is not defined` из сохранённого кэша; для такого кэша выводится понятное сообщение о необходимости перезапуска проверки.
- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP/CIDR, быстрый relay и пассивное SNI-определение не переписывались.

## 1.25.19

- Исправлено отображение маршрута SOCKS5/SNI-соединений для доменов, которые маршрутизируются через активные удалённые rule-set Re:filter/Sing-box, но не внесены вручную в список «Всегда через VPN».
- Если SOCKS5-клиент передал IP, а домен восстановлен через TLS SNI, backend больше не помечает такой домен как уверенный DIRECT только из-за отсутствия ручного правила.
- Добавлена причина `remote_rule_set_sni_candidate` и пояснение, что маршрут определяется активным remote rule-set Sing-box.
- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP/CIDR и relay длинных потоков не изменялись.

## 1.25.18

- Добавлен безопасный пассивный SNI-tap для SOCKS5/IP-направлений.
- Улучшена диагностика реального клиента: SNI-домены, исходный IP, счётчик SNI-событий.
- Улучшено отображение соединений: для SNI показывается домен и исходный IP-адрес.
- Рабочие hot path функции HTTP/SOCKS5, trusted bypass и relay не переписывались.

# v1.25.17 — безопасная диагностика фактического выхода SOCKS5/IP

- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP/CIDR и relay длинных потоков не изменялись.
- Исправлена логика отображения маршрута для SOCKS5-соединений, где клиент передал IP-адрес вместо домена: такие соединения больше не помечаются как уверенный `DIRECT` по одному только default-правилу.
- Добавлены признаки `destination_type`, `route_certainty`, `expected_route` для gateway-событий и клиентских соединений.
- В диагностике реального клиента добавлены счётчики IP-назначений и неподтверждённых маршрутов, а также пояснение, что доменное правило VPN не может быть подтверждено по одному IP.
- В списке соединений и диагностике клиента теперь отображается тип назначения (`домен` / `IP`) и предупреждение `фактический выход не подтверждён`, если маршрут нельзя подтвердить без домена/SNI.
- `proxy-gateway` по-прежнему не попадает в «Основные сайты» и группируется как неизвестное назначение.

# v1.25.16 — безопасная диагностика реального клиента и очистка SOCKS5-отображения

- Добавлена вкладка диагностики «Клиент»: выбор онлайн-клиента, запуск наблюдения и проверка реальных входящих HTTP/SOCKS-событий без изменения рабочего трафика.
- Добавлен backend endpoint `/api/diagnostics/client_watch` для анализа gateway-событий выбранного клиента: вход, trusted/auth, назначение, маршрут, трафик, активность.
- `proxy-gateway` больше не попадает в «Основные сайты» в списке соединений; неизвестные SOCKS5/HTTP назначения группируются отдельной строкой, не засоряя список сайтов.
- Рабочий транспорт HTTP 2081, SOCKS5 2080, trusted IP/CIDR и relay длинных потоков не изменялись.
- Обновлена документация по диагностике реального клиента.

## 1.25.15

- Стабилизационный патч по зафиксированным регрессиям интерфейса, диагностики и клиентской телеметрии.
- Исправлена диагностика задержек: `direct_http_204` больше не использует необъявленный `server_version`, ошибки показываются как `timeout`, `DNS error`, `connection refused`, `HTTP status` или понятная причина теста.
- Упрощена шапка клиента: убраны дублирующие бейджи `HTTP proxy` / `HTTP/SOCKS` / `SOCKS5 proxy` / `Trusted HTTP/SOCKS без auth`; оставлены только статус, trusted без auth и реально активные входы.
- Убрана повторяющаяся подпись `Трафик идёт напрямую через Raspberry/провайдера...` из каждой строки соединения. Маршрут теперь самодостаточно показывает `DIRECT` или `VPN: <сервер>`.
- Исправлено отображение `Основные сайты`: IP-адреса с портами больше не обрезаются до хвостов вроде `253.33` / `32.47`; показывается полный IP без порта или нормальный домен.
- Сохранена изоляция технических raw-hop соединений sing-box, чтобы `SE1-vless → Proxy` и служебные соединения не подменяли реальные клиентские карточки.
- Сохранены рабочие HTTP 2081, SOCKS5 2080, trusted IP/CIDR и быстрый relay длинных потоков.
- README обновлён под единую вкладку Диагностика и стабилизационные исправления.

## 1.25.12

- Объединена диагностика: старая вкладка «Настройки → Диагностика» перенесена в главную вкладку **Диагностика** как подраздел «Логи и режим». В настройках отдельная диагностика больше не дублируется.
- Убраны лишние быстрые переходы из диагностики в «Соединения» и «Логи», чтобы не плодить мусор в интерфейсе.
- Добавлены IP-показатели в шапку и обзор: IP подключения к UI, IP сервера напрямую и IP через Proxy из последней проверки маршрута.
- В диагностике маршрутов используются реальные правила маршрутизации: ручные домены «Всегда через VPN», включённые пресеты и исключения. Статичные api.ipify/google/cloudflare больше не используются как основной список маршрутов.
- Исправлен экспорт отчёта диагностики: при недоступном `navigator.clipboard` в Home Assistant Ingress отчёт показывается в текстовом поле для ручного копирования.
- Исправлена классификация соединений: raw-hop строки внутреннего sing-box скрываются из основного списка, если есть синтетическая gateway-строка реального клиента. Строки «Служебное соединение add-on» больше не должны подменять сайты клиента.
- Для trusted SOCKS5 сохранён прозрачный путь v1.25.2/v1.25.10; добавлена только пассивная телеметрия назначения без изменения байтов клиента.
- Рабочая логика HTTP 2081, SOCKS5 2080 и relay длинных потоков не менялась.

## v1.25.11 - восстановление стабильного SOCKS5 для роутеров

- Восстановлен стабильный прозрачный SOCKS5-путь для trusted IP/CIDR по логике рабочей версии 1.25.2.
- Убрано вмешательство телеметрии в hot path SOCKS5 trusted-подключений.
- Сохранены исправления длинного relay, диагностика, маршруты, история и отображение клиентов.
- HTTP 2081 не изменялся.

## 1.25.11

- Восстановлен прозрачный SOCKS5 passthrough для trusted IP/CIDR по стабильной логике v1.25.2: trusted-роутер снова проходит напрямую к внутреннему sing-box SOCKS без пересборки SOCKS5-handshake.
- Добавлена пассивная телеметрия назначения SOCKS5: домен/порт считываются из потока без изменения байтов клиента и без вмешательства в relay.
- Сохранены исправления v1.25.4 по скорости и стабильности длинных потоков, а также вкладка **Диагностика** из v1.25.8.
- HTTP `2081` не менялся по рабочей логике; исправление сфокусировано на регрессии SOCKS5 `2080` для роутеров/Keenetic и trusted IP.

## 1.25.8

- Исправлена SOCKS5-ветка 2080: публичный gateway теперь открывает внутренний SOCKS5 по разобранному назначению и отдаёт клиенту стандартный SOCKS5 success reply, не прокидывая upstream bind-address наружу. Приоритет — стабильность роутеров/Keenetic и trusted IP без auth.
- Добавлена отдельная вкладка **Диагностика**: статус портов HTTP/SOCKS5/Telegram/internal, trusted bypass, активный VPN-сервер, проверка маршрута URL, сравнение HTTP 2081 / SOCKS5 2080 / внутреннего VPN-core и экспорт отчёта.
- Диагностика убрана с вкладки **Обзор** и перенесена в отдельный раздел, чтобы главная страница не была перегружена техническими проверками.
- Проверка длинного потока теперь сравнивает HTTP gateway, SOCKS5 gateway и internal VPN-core, чтобы отличать проблему gateway от проблемы VPN-ноды или внешнего сайта.

# Changelog

## 1.25.7

- Исправлен критический регресс SOCKS5-входа `2080`, из-за которого клиенты с доменным SOCKS5-запросом получали `connection to proxy closed`.
- Причина: некорректное декодирование домена через Python `idna` с `errors=ignore` в телеметрии SOCKS5. Телеметрия больше не ломает handshake.
- Сохранён trusted auth bypass для доверенных IP/CIDR на SOCKS5 и HTTP.
- Сохранены исправления стабильного relay длинных потоков и отображения HTTP/SOCKS клиентов.
- Добавлены smoke-проверки декодирования SOCKS5-доменов и trusted SOCKS5 path.

## 1.25.6

- Исправлена регрессия v1.25.5, из-за которой реальные HTTP/SOCKS подключения через auth-gateway могли пропадать из блока **Клиенты онлайн** сразу после завершения запроса, а в интерфейсе оставалась только внутренняя строка sing-box вида `SE1-vless → Proxy`.
- Добавлен кратковременный recent-кэш gateway-подключений: быстрые запросы и проверки теперь остаются видимыми в UI и истории достаточно долго, чтобы автообновление их не пропускало.
- Внутренние raw-hop строки `auth-gateway → internal SOCKS/sing-box` больше не подменяют основную клиентскую строку. Основной источник данных для клиента — синтетическая gateway-запись с реальным IP клиента, trusted/auth-меткой, доменом назначения и финальным маршрутом.
- Реальные сайты клиентов снова отображаются в карточках клиентов и в истории доменов/IP; `proxy-gateway` используется только как внутренний fallback, когда назначения действительно нет.
- **Служебные соединения add-on** остаются отдельным раскрывающимся блоком и не смешиваются с клиентскими сайтами.
- Сохранена правка v1.25.4 по устойчивому relay длинных потоков; новых изменений в fast path маршрутизации не добавлялось.
- Удалены продуктовые упоминания и функции, привязанные к частному сервису/консоли; диагностика остаётся универсальной.

## 1.25.4

- Главная диагностика стала универсальной: вместо отдельной логики под конкретный сервис добавлен общий инструмент **Диагностика Proxy/VPN**.
- Добавлена **Проверка стабильности потока** по произвольному URL: проверяются два участка — внешний `auth-gateway` и внутренний `VPN-core/sing-box`.
- Диагностика даёт продуктовый вывод: проблема в маршруте, gateway/relay, внутреннем proxy/VPN, выбранной ноде или во внешнем сервисе.
- Исправлен relay `auth-gateway` для длинных потоков: non-blocking `sendall()` заменён на устойчивый двунаправленный blocking-relay с отдельными потоками и корректным half-close. Это должно устранить случайные обрывы больших HTTP/HTTPS-загрузок.
- Из главной диагностики убрана кнопка и жёсткая привязка к частному сервису; любые сервисы теперь проверяются через ручной ввод домена/URL.
- Сохранены улучшения v1.25.3: понятный маршрут соединений `auth-gateway → Proxy → VPN/DIRECT`, предупреждения по DIRECT и метка **trusted_ip без auth**.

## 1.25.3

- Добавлена проверка маршрута **Proxy/VPN** на главной странице: прямой внешний IP Raspberry, внешний IP через внутренний proxy, активный VPN-сервер, статус VPN и таблица доменов с решением `Proxy/VPN` или `DIRECT`.
- Добавлена кнопка **Проверить маршрут сейчас**: диагностика запускается из интерфейса и показывает, почему трафик пошёл напрямую или через VPN.
- Соединения auth-gateway теперь отображают полный понятный путь: `auth-gateway → Proxy → VPN: выбранный сервер` или `auth-gateway → Proxy → DIRECT`, с предупреждением, если финальный маршрут прямой.
- В карточках клиентов и активных соединениях восстановлена явная метка **trusted_ip без auth** для доверенных IP/CIDR, которые проходят без логина и пароля.
- Для синтетических соединений auth-gateway добавлен подсчёт Down/Up во время relay, чтобы активные HTTP/SOCKS соединения не выглядели как вечные `0 B / 0 B`.
- Добавлен backend endpoint `/api/route/diagnostics` для быстрой диагностики маршрута без ручных `curl`-проверок.
- Fast path доступа не усложнялся: проверка trusted/auth остаётся быстрой, а диагностика выполняется отдельным запросом и не должна задерживать клиентский трафик.

## 1.25.2

- Исправлено сопоставление автоматического трафика подписки с источниками серверов после миграции старых серверов: legacy/ранее добавленные серверы больше не должны ошибочно показываться как источник без автоматического трафика, если провайдер реально отдаёт лимит.
- Кнопка **Обновить трафик подписки** теперь возвращает не только `subscription_info`, но и актуальную модель `traffic_sources`, поэтому после нажатия обновляются вкладка **Трафик** и блок **VPN-подписка и лимит трафика** во вкладке **VPN-серверы**.
- Добавлен fallback **Трафик текущей подписки**: если провайдер отдал лимит, но источник невозможно однозначно сопоставить, данные отображаются отдельной строкой и не теряются.
- Во вкладке **VPN-серверы** блок подписки дополнен мини-сводкой по источникам трафика, чтобы информация совпадала со вкладкой **Трафик**.
- Fast path HTTP/SOCKS gateway не менялся: по замеру задержка через прокси была близка к прямому запросу, поэтому исправление сосредоточено на отображении и обновлении трафика.

## 1.25.1

- Вкладка **Безопасность** вынесена в главное меню и разбита на человекопонятные подвкладки: доверенный доступ, авторизация и порты, геофильтр RU, автоблокировка, блокировки и события.
- Вкладка **Блокировки** сохранена как основной рабочий список блокировок; в безопасности добавлена сводка и быстрый переход без дублирования источников правды.
- Вкладка **Трафик** переработана по источникам серверов: основная подписка, дополнительные подписки, JSON/ручные серверы и ручные лимиты отображаются отдельно.
- Исправлено отображение **Соединений** для HTTP/SOCKS auth-gateway: реальные внешние клиенты и назначения больше не должны скрываться только как служебные localhost-соединения.
- Обновлены README и инструкции под фактическую структуру интерфейса.

## 1.24.4

- Исправлен HTTP auth-gateway на штатных портах: обычные HTTP proxy-запросы и HTTPS `CONNECT` больше не должны сбрасываться gateway до передачи во внутренний `sing-box`.
- Внешняя схема остаётся без дополнительных trusted-портов: доверенные IP/CIDR работают без авторизации на штатных HTTP/SOCKS5 портах, остальные IP проходят через штатную авторизацию.
- HTTP gateway теперь сам обрабатывает `CONNECT` и обычные HTTP proxy-запросы, подключаясь к внутреннему `sing-box` через localhost SOCKS5.
- MTProto не затронут правилами HTTP/SOCKS trusted bypass: порт MTProto и идентификация пользователей по secret остаются отдельной логикой.
- Усилена проверка: старые trusted-порты 2085/2086 не используются, служебные поля приложения не попадают в конфиг `sing-box`.

## 1.24.3

- Переделан trusted-доступ без авторизации: дополнительные порты 2085/2086 удалены. Доверенные IP/CIDR теперь работают без логина/пароля на штатных HTTP/SOCKS5 портах.
- Для неизвестных IP штатная авторизация HTTP/SOCKS5 остаётся обязательной.
- Добавлен локальный auth-gateway перед sing-box: внешние порты остаются прежними, внутренний sing-box слушает localhost-порты.
- Добавлены массовые действия с клиентами: выбор галочками, удаление из списка, блокировка, доверие/снятие доверия, отключение активных соединений.
- Обновлены инструкции, README и тесты под новую схему без дополнительных trusted-портов.

## 1.24.2

- Добавлен доверенный доступ без авторизации для IP/CIDR: отдельные trusted HTTP/SOCKS5-порты без логина и пароля, ограниченные allowlist.
- Основные HTTP/SOCKS5-порты с авторизацией не меняются; неизвестные IP на trusted-портах блокируются.
- Во вкладку **Безопасность** добавлен блок настройки trusted bypass: включение, HTTP/SOCKS режимы, список доверенных IP/CIDR и пояснение для ZeroOmega.
- Во вкладку **Порты** добавлены trusted HTTP/SOCKS порты и подсказка по пробросу.
- `/api/security`, `/api/ports`, `/api/status` и инструкции обновлены под новую модель доступа.
- Smoke-test проверяет, что trusted-настройки не попадают в `sing-box` как неизвестные поля, trusted inbounds создаются без пользователей, а доступ ограничивается `source_ip_cidr`.

## 1.24.1

- Hotfix: исправлена критическая ошибка v1.24.0, из-за которой служебное поле приложения `use_in_auto` попадало в `/tmp/boris-proxy/sing-box.json` и ломало запуск `sing-box` с ошибкой `json: unknown field "use_in_auto"`.
- Добавлен общий список служебных metadata-полей VPN-серверов, которые вычищаются перед генерацией чистого outbound-конфига `sing-box`.
- Усилен smoke-test: проверяется, что `enabled`, `use_in_auto`, `source_id`, `source_name`, `source_type`, `source_url` и другие внутренние поля приложения не попадают в outbounds `sing-box`.
- README очищен от устаревшего блока «Что нового в 1.22.0»: история версий остаётся в CHANGELOG, README описывает актуальную логику приложения.

## 1.24.0

- Добавлен product-hardening контроль: серверы, источники, журналы, кэши, обслуживание и инструкции проверяются как единая система.
- В событиях и аудите включена глобальная санитарная очистка чувствительных данных: URL подписок, пароли, secret, token, uuid и похожие поля маскируются перед записью.
- В обслуживание добавлены системный `/data/options.json`, кэш `/data/rules` и агрегированная активность клиентов `/data/client_activity.json`.
- Для каждого VPN-сервера добавлены отдельные переключатели: включён/выключен и участвует/не участвует в auto.
- Для источников серверов добавлен переключатель включения/выключения всего источника, отдельно от участия источника в auto.
- Итоговый auto-пул теперь учитывает включённость источника, включённость сервера, разрешение auto на источнике и разрешение auto на самом сервере.
- История дополнена агрегированной активностью клиентов по доменам/IP без перехвата содержимого HTTPS.
- Инструкции дополнены описанием полного контроля серверов, кэша rule-set маршрутизации и агрегированной активности клиентов.
- Smoke-test усилен проверками новых endpoint'ов, registry-файлов, санитарной очистки и переключателей серверов/источников.

## 1.23.2

- Проведена продуктовая доводка взаимосвязей после добавления источников серверов и контроля файлов данных.
- Раздел **Инструкции** переделан в раскрываемые группы, чтобы справка не была одним большим массивом текста.
- В инструкции добавлен отдельный раздел **Хранение данных, журналы и очистка**: описаны история, MTProto activity, аудит, технические события, геокэш, автобан-счётчики и автобэкапы `servers.json`.
- Раздел **Обслуживание** теперь показывает не только путь/размер файла, но и человеческие пояснения: зачем файл нужен, содержит ли секреты, входит ли в backup, как очищается и что пользователю можно с ним делать.
- Backend отдаёт для каждого maintenance-файла поля `purpose`, `backup_policy`, `cleanup_policy`, `user_action` и `growth_control`.
- Smoke-test усилен: проверяет, что каждый файл из DATA_REGISTRY имеет пользовательское описание, backup-политику, cleanup-политику и действие для пользователя; потенциально растущие файлы обязаны иметь cleanup.
- README обновлён разделом о хранении данных, журналах, лимитах и обслуживании.

## 1.23.1

- Проведена ревизия связей после добавления источников VPN-серверов.
- Исправлены пути в DATA_REGISTRY: доверенные клиенты теперь связаны с реальным `trusted_clients.json`, Telegram MTProto — с `telegram_proxy.json`.
- В обслуживание добавлены ранее неучтённые файлы: `clients.json`, `geo_cache.json`, `security_autoban_events.json` и автобэкапы `servers.backup.*.json`.
- Добавлены cleanup-правила для кэша клиентов, кэша геолокации, временных счётчиков автобана и автобэкапов серверов.
- Автобэкапы `servers.json` теперь ограничены последними 5 файлами, чтобы они не росли бесконечно.
- Исправлена очистка устаревших клиентов: старые offline-записи теперь действительно удаляются из `clients.json` по сроку хранения.
- Усилен smoke-test: проверяет, что каждый JSON-файл, используемый через `data_path(...)`, зарегистрирован в DATA_REGISTRY и не может быть забыт в обслуживании/cleanup.

## 1.23.0

- Добавлена модель источников VPN-серверов: подписка, JSON-импорт, импорт ссылок, ручные и ранее добавленные серверы.
- Добавлен файл `/data/server_sources.json` с включением в DATA_REGISTRY, backup, обслуживание и cleanup.
- Во вкладке **VPN-серверы** добавлено представление **По источникам**: серверы сгруппированы по подпискам/JSON/ручным источникам.
- В общий список серверов добавлена колонка **Источник**.
- Добавлена возможность удалить отдельный сервер и удалить все серверы конкретного источника.
- Добавлен переключатель участия источника серверов в `auto`.
- Удаление серверов очищает связанные ping-данные и JSON подписки, но не стирает исторические записи сессий.
- История маршрутов теперь показывает источник текущего сервера либо пометку, что маршрут исторический/удалённый.
- README обновлён под новую модель источников серверов.
- Smoke-test расширен проверками `server_sources`, UI-связок и backend API.

## 1.22.1

- Переработан верхний статус add-on: горизонтальный блок в шапке теперь отдельно показывает состояние сервиса, VPN-сервер, пинг, MTProto, количество онлайн-клиентов и режим работы.
- Статус «Система» заменён на более понятный общий health сервиса: «работает / частично / запуск / ошибка».
- Исправлена навигация в «Настройках»: карточки разделов получили стабильную вёрстку, описание больше не слипается с заголовком после возврата назад.
- История подключений теперь сохраняет раскрытые карточки клиентов при автообновлении и не сворачивается каждые несколько секунд.
- Усилен startup flow add-on: backend поднимает web-интерфейс раньше и отдаёт bootstrap/health состояние, чтобы снизить вероятность ошибки Ingress сразу после обновления.
- `/api/health` и `/api/status` теперь отдают состояние запуска add-on, а UI ждёт готовности сервисов перед полной загрузкой тяжёлых данных.

## 1.22.0

- Переработан статус Telegram MTProto: онлайн теперь считается по активности конкретного secret с grace-window 90 секунд, а не только по мгновенному TCP-соединению.
- Добавлен файл `/data/mtproto_activity.json` с безопасным учётом активности по пользователям без хранения полного secret в журнале активности.
- Во вкладке Telegram теперь отображаются последняя активность, источник идентификации, активные соединения и количество событий активности.
- Веб-интерфейс грузит основные данные быстро: сначала статус/серверы/пользователей/маршруты, тяжёлые данные догружаются в фоне.
- `/api/status` больше не запускает тяжёлое обновление соединений при каждом открытии интерфейса, а использует кэш мониторинга.
- Добавлена диагностика задержек `/api/network/quality`: проверка прямого TCP/HTTP и реального HTTP-delay через VPN-серверы sing-box.
- В настройках → Диагностика появилась кнопка «Проверить задержки».

## 1.22.0

- Удалены дублирующиеся top-level функции backend: теперь smoke-test падает, если в коде снова появятся перезаписывающиеся определения.
- Добавлен расширенный API-contract smoke-test: все литеральные `/api/...` вызовы интерфейса проверяются на наличие backend-роута.
- Аудит отделён от технического лога: `/data/audit.json` для событий доступа/безопасности, `/data/events.json` для технических событий.
- Раздел обслуживания теперь показывает размер отдельного audit-журнала и лимит его хранения.
- Усилен первый запуск: диагностика предупреждает о дефолтном secret, дефолтном proxy password, неполной авторизации и отсутствии пользователей.
- В `config.yaml` включён `panel_admin: true`, а HTTP proxy auth теперь включён по умолчанию.
- Добавлен runtime `production_mode`: обычный, диагностика, отладка и безопасный режим.
- Безопасный режим блокирует изменяющие API-операции, кроме смены режима, обслуживания и очистки аудита.
- Прямые перезапуски sing-box/MTProto в пользовательских API заменены фоновым применением там, где это возможно.
- Dockerfile возвращён на проверенную схему с `ghcr.io/sagernet/sing-box:latest` и `ghcr.io/dolonet/mtg-multi:latest`, чтобы Home Assistant снова собирал add-on без fragile source-build mtg-multi.

## 1.20.1
- Исправлены отсутствующие API `/api/audit` и POST `/api/routing/test`, из-за которых аудит и проверка маршрута возвращали 404.
- Пароли и MTProto secrets теперь раскрываются по клику и снова скрываются по повторному клику.
- Исправлена верстка карточек в настройках: заголовок и описание больше не слипаются.
- В обслуживании путь пользователей исправлен на реальный `/data/proxy_users.json`.
- Расширены smoke-тесты на frontend/backend API-связки.

## 1.20.0

- Крупная версия стабильности и безопасности.
- Добавлена схема миграций данных `/data/migrations.json`, чтобы старые `users.json` и runtime-настройки не ломались после обновлений.
- Добавлен безопасный apply sing-box: предварительная проверка `sing-box check`, сохранение последнего рабочего конфига и rollback при ошибке нового конфига.
- Добавлен раздел `Настройки → Аудит` для чистого журнала событий доступа, пользователей, безопасности и серверов.
- Добавлен раздел `Настройки → Обслуживание`: размер файлов данных, история, аудит, свободное место, ручная очистка по правилам хранения.
- Добавлена проверка маршрута домена/IP во вкладке `Маршрутизация`.
- Добавлена ротация доступа пользователя: смена пароля и Telegram secret одной операцией.
- Пользователи получили поля группы, срока действия, masked-поля пароля/secret в интерфейсе.
- Резервная копия теперь может создаваться с секретами или с замаскированными секретами для безопасной передачи в поддержку.
- Расширены smoke-проверки перед сборкой: Python import, основные функции, JS syntax, YAML, соответствие HTML id.

## 1.19.15

- Добавлен раздел `Настройки → Инструкции`.
- Исправлен текст возле переключателей пользователей и Telegram.
- Доработано отображение обновления трафика.
- Добавлены инструкции по Keenetic Proxy Client, маршрутизации и DNS.
